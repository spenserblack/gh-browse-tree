# `gh browse-tree`

## Description

This GitHub CLI extension provides an interactive prompt for browsing the file tree of a repository.

## Install

```shell
gh extension install spenserblack/gh-browse-tree
```
